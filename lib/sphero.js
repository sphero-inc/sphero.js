/**
 * Connects to the Sphero-compatible device.
 *
 * @param {String} conn port/UUID of the device to connect to
 * @param {Object} opts options
 * @return {Object} connection
 */
module.exports.connect = function connect(conn, opts) {
  if (opts == null) { opts = {}; }
};
